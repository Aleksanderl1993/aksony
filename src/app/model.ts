export interface Age {
    id: number;
    age: string;
    photoUrl: string;
}

export interface Material {
    type: string;
    photoUrl: string;
}

export interface ToyFunction {
    type: string;
    description: string;
}
